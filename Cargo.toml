
[workspace]
members = [
    "egui-frontend",
    "fits_inspect",
    "indi",
    "phd2",
    "twinkle_client",
    "twinkle_server",
]
resolver = "2"

[profile.release]
opt-level = 2 # fast and small wasm

# Optimize all dependencies even in debug builds:
[profile.dev.package."*"]
opt-level = 2
